Run quality checks and auto-fix formatting issues.

1. Run `make check` to execute luacheck and stylua checks
2. If stylua reports formatting issues, run `make format` to auto-fix
3. Re-run `make check` to verify all checks pass
4. Report results
